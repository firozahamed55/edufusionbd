import { ResultProcessor } from "../../components/ResultProcessor";

/** Exam · Result Sheet Download — view processed results by section. */
export function ResultSheetDownloadScreen() {
  return <ResultProcessor mode="view" />;
}
