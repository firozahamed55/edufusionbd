import { ResultProcessor } from "../../components/ResultProcessor";

/** Exam · Result Process — compute GPA/grade/merit from marks. */
export function ResultProcessScreen() {
  return <ResultProcessor mode="process" />;
}
