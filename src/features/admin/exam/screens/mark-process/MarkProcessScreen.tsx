import { ResultProcessor } from "../../components/ResultProcessor";

/** Exam · Mark Process — run result processing from entered marks. */
export function MarkProcessScreen() {
  return <ResultProcessor mode="process" />;
}
