import { MarksEntry } from "../../components/MarksEntry";

/** Exam · Mark Update — live marks update (upserts existing). */
export function MarkUpdateScreen() {
  return <MarksEntry mode="update" />;
}
