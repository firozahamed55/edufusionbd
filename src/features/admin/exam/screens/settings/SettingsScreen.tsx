import { ExamSettingsTab } from "../../components/ExamSettingsTab";

/** Exam · Settings (exam-start tab) — live exam create/manage. */
export function SettingsScreen() {
  return <ExamSettingsTab />;
}
