import { AttendanceMarker } from "../../components/AttendanceMarker";

/** Attendance · Section — live daily attendance marking. */
export function SectionScreen() {
  return <AttendanceMarker context="daily" />;
}
