import { AttendanceMarker } from "../../components/AttendanceMarker";

/** Attendance · Exam — live exam attendance marking. */
export function ExamScreen() {
  return <AttendanceMarker context="exam" />;
}
