import { CertRecordForm } from "../../components/CertRecordForm";

/** Certificate · Testimonial — live record creation. */
export function TestimonialScreen() {
  return <CertRecordForm kind="testimonial" />;
}
