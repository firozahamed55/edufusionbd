import { CertRecordForm } from "../../components/CertRecordForm";

/** Certificate · Transfer Certificate — live record creation. */
export function TransferScreen() {
  return <CertRecordForm kind="transfer" />;
}
