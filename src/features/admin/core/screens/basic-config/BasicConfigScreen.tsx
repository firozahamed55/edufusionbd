import { InstitutionForm } from "../../components/InstitutionForm";

/** Core · Basic Settings — live institution profile edit. */
export function BasicConfigScreen() {
  return <InstitutionForm mode="basic" />;
}
