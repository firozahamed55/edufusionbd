import { MigrationRunner } from "../../components/MigrationRunner";

/** Student · Migration (Merit) — promote in merit order. Live via MigrationRunner. */
export function MigrationMeritScreen() {
  return <MigrationRunner type="merit" />;
}
