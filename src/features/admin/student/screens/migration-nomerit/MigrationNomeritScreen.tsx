import { MigrationRunner } from "../../components/MigrationRunner";

/** Student · Migration (No Merit) — whole-class promotion. Live via MigrationRunner. */
export function MigrationNomeritScreen() {
  return <MigrationRunner type="no_merit" />;
}
