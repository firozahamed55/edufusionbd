import { TeacherForm } from "../../components/TeacherForm";

/**
 * Teacher · Update Profile — Figma (Light 161:2 / Dark 165:2).
 * Thin wrapper over the shared TeacherForm (update mode).
 */
export function UpdateProfileScreen() {
  return <TeacherForm mode="update" />;
}
