import { TeacherForm } from "../../components/TeacherForm";

/**
 * Teacher · Registration — Figma (Light 139:2 / Dark 143:2).
 * Thin wrapper over the shared TeacherForm (register mode).
 */
export function RegistrationScreen() {
  return <TeacherForm mode="register" />;
}
